import 'package:flutter/material.dart';

class SpmExplainScreen extends StatelessWidget {
  const SpmExplainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
