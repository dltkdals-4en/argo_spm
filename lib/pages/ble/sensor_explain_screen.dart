import 'package:flutter/material.dart';

class SensorExplainScreen extends StatelessWidget {
  const SensorExplainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
