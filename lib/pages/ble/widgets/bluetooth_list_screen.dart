import 'package:flutter/material.dart';

class BluetoothListScreen extends StatelessWidget {
  const BluetoothListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
